
        // atom built in
var   atom = require('atom')

module.exports = {

    init: function() {

    },

    dispose: function() {

    },

    lint_file: function(editor, temporary_path, done) {
        done([]);
    }
}
